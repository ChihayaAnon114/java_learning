- 向上转型：子类 --> 父类

~~~java
Developer d = new Developer();
Employee e = d; //向上转型 系统自动完成的
~~~

- 向下转型：父类 --> 子类
    - instanceof关键字：对象 instanceof 类/接口，判断对象是不是某种类型或者实现了某个接口**

~~~java
if (pet instanceof Dog) {
    ((Dog) pet).method();
}
~~~

**- 向下转型的应用场景：需要调用子类特有的方法时。**